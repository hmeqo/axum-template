// Server-side session authentication (future).
// When implemented, this will provide session-based auth
// as an alternative to JWT, using cookie-based session IDs
// with server-side storage (e.g. Redis or DB).
